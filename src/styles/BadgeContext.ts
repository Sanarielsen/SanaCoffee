import styled from 'styled-components';

export const BadgeContext = styled.p`
  display: flex;
  padding: 4px;
  align-items: center;
  font-family: 'Roboto';
  font-style: normal;
  font-weight: 400;

  font-size: clamp(12px, 1vw, 18px);
  
  line-height: 130%;
  color: #4b2995;
`;
